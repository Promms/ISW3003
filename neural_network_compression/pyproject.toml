[project]
name = "neural_network_compression"
version = "0.1.0"
description = "Model factorization and structured and unstructured pruning comparison"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "detectors>=0.1.11",
    "fvcore>=0.1.5",
    "timm>=1.0.27",
    "torch>=2.12.0",
    "torchvision>=0.27.0",
    "tqdm>=4.66.0",
]
