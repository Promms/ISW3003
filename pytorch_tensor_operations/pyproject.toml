[project]
name = "pytorch_tensor_operations"
version = "0.1.0"
description = "PyTorch tensor reshaping, broadcasting, pairwise operations, and patch transformations"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.3",
    "torch>=2.10.0",
    "torchvision>=0.25.0",
]
