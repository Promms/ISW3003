[project]
name = "pytorch_model_internals"
version = "0.1.0"
description = "PyTorch parameters, buffers, weights, freezing, hooks, and state management exercises"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.3",
    "torch>=2.10.0",
    "torchvision>=0.25.0",
]
