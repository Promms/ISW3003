[project]
name = "model_architecture_and_flops_analysis"
version = "0.1.0"
description = "Deep learning model implementation with parameter and FLOPs analysis"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.4",
    "torch>=2.11.0",
]

[tool.ruff]
line-length = 200 # Sets the line length to 120 characters
