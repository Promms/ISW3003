[project]
name = "llm_json_extraction"
version = "0.1.0"
description = "Structured JSON extraction from announcements using a small language model"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.12.0",
    "transformers>=5.10.2",
    "tqdm>=4.66",
]
